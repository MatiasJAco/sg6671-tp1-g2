#include <vector>
using namespace std;

vector<float> calcularPuntos(vector<float> & controlPoints);

vector<float> calcularPuntosEquiespaciados2D(vector<float> & controlPoints, float paso, float tolerancia=0.02);
